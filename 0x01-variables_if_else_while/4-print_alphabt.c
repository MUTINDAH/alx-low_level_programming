#include <stdio.h>
/**
 * main-entry point of program
 * return-success return 0
 */
int main (void)
{
	char ch;
	for (ch = 0;ch <= 0; ch++)
	{
		if ( ch != 'e' && ch != 'q')
			putchar(ch);
	}
	return (0);
}
